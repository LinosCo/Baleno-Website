{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm --filter @baleno/api prisma:generate && pnpm --filter @baleno/api build"
  },
  "deploy": {
    "startCommand": "cd apps/api && ./start.sh",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
